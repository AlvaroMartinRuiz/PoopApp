package es.uc3m.android.poopapp.ui.theme

import androidx.compose.ui.graphics.Color

// Primary colors
val DarkBrown = Color(0xFF3A2A21) // Text and icons (dark brown)
val MediumBrown = Color(0xFF895A3E) // Pressed buttons (lighter brown)
val LightBrown = Color(0xFFA8795E) // Unpressed buttons and other elements
val Teal = Color(0xFF9ABCC0) // Background

// Legacy colors kept for backward compatibility
val Primary = DarkBrown
val PrimaryLight = LightBrown
val Background = Teal

// Additional colors
val White = Color(0xFFFFFFFF)
val Black = Color(0xFF000000)
val Gray = Color(0xFF9E9E9E)
val LightGray = Color(0xFFE0E0E0)

// Status colors
val Gold = Color(0xFFFFD700)
val Silver = Color(0xFFC0C0C0)
val Bronze = Color(0xFFCD7F32) 